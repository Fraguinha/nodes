#cloud-config

hostname: ${HOSTNAME}
manage_etc_hosts: true
timezone: Europe/Lisbon

users:
  - name: pi
    groups: [adm, sudo]
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIIF0LgvmQ2s7H/x3MZ8Frv3XGfNj/6sqIg08tiCWB4Cd

bootcmd:
  - until ping -c1 -W2 1.1.1.1 &>/dev/null; do sleep 2; done

package_update: true
package_upgrade: true
packages:
  - curl
  - iw
  - jq
  - linux-firmware-realtek
  - nfs-common
  - nfs-kernel-server
  - open-iscsi
  - unattended-upgrades
  - wpasupplicant

apt:
  conf: |
    APT::Periodic::Update-Package-Lists "1";
    APT::Periodic::AutocleanInterval "5";
    APT::Periodic::Unattended-Upgrade "1";
    APT::Periodic::Verbose "1";
    Unattended-Upgrade::Remove-Unused-Dependencies "true";
    Unattended-Upgrade::Remove-New-Unused-Dependencies "true";
    Unattended-Upgrade::AutoFixInterruptedDpkg "true";

swap:
  size: 0

write_files:
  - path: /etc/k3s-resolv.conf
    content: |
      nameserver 1.1.1.1
      nameserver 1.0.0.1

  - path: /etc/sysctl.d/99-tailscale.conf
    content: |
      net.core.rmem_max=7340032
      net.core.wmem_max=7340032
      fs.inotify.max_user_instances=512
      fs.inotify.max_user_watches=65536

  - path: /etc/systemd/network/10-wlan0.link
    content: |
      [Match]
      Type=wlan

      [Link]
      Name=wlan0

  - path: /etc/udev/rules.d/70-wifi-powersave-off.rules
    content: |
      ACTION=="add", SUBSYSTEM=="net", KERNEL=="wlan0", RUN+="/usr/sbin/iw dev wlan0 set power_save off"

  - path: /etc/chrony/conf.d/ip-fallback.conf
    content: |
      server 162.159.200.1 iburst
      server 162.159.200.123 iburst
      makestep 1.0 3

  - path: /etc/systemd/system/k3s-agent.service.d/restart-after-tailscale.conf
    content: |
      [Unit]
      PartOf=tailscaled.service
      After=tailscaled.service

  - path: /etc/systemd/system/node-health.service
    content: |
      [Unit]
      Description=Node health check
      After=tailscaled.service k3s-agent.service

      [Service]
      Type=exec
      ExecStart=/usr/local/bin/node-health-check.sh
      Restart=always
      RestartSec=5min

      [Install]
      WantedBy=multi-user.target

  - path: /usr/local/bin/node-health-check.sh
    permissions: "0755"
    content: |
      #!/usr/bin/env bash
      set -euo pipefail

      if ! tailscale status &>/dev/null; then
        systemctl restart tailscaled
        exit 0
      fi

      if systemctl is-active k3s-agent &>/dev/null && ! ip link show flannel.1 &>/dev/null; then
        systemctl restart k3s-agent
        exit 0
      fi

      exit 0

  - path: /usr/local/bin/setup-k3s.sh
    permissions: "0755"
    content: |
      #!/usr/bin/env bash
      set -euo pipefail

      wait_for() { until "$@" &>/dev/null; do sleep 5; done; }
      retry() {
        local out=""
        while [[ -z "$out" ]]; do
          out=$("$@" 2>/dev/null || true)
          [[ -z "$out" ]] && sleep 10
        done
        echo "$out"
      }
      resolve_peer_ip() {
        tailscale status --json | jq -r --arg h "$1" '.Peer[] | select(.HostName == $h) | .TailscaleIPs[0]'
      }

      wait_for tailscale status
      TAILSCALE_IP=$(tailscale ip -4)
      SERVER_IP=$(retry resolve_peer_ip "${SERVER_HOSTNAME}")
      K3S_TOKEN=$(retry ssh -o StrictHostKeyChecking=no "pi@${SERVER_IP}" "sudo cat /var/lib/rancher/k3s/server/node-token")

      curl -sfL https://get.k3s.io | sh -s - agent \
        --server "https://${SERVER_IP}:6443" \
        --token "${K3S_TOKEN}" \
        --node-ip "${TAILSCALE_IP}" \
        --node-external-ip "${TAILSCALE_IP}" \
        --flannel-iface tailscale0 \
        --resolv-conf /etc/k3s-resolv.conf \
        --kubelet-arg "allowed-unsafe-sysctls=net.ipv4.ip_forward"

runcmd:
  - until ping -c1 -W2 1.1.1.1 &>/dev/null; do sleep 5; done
  - curl -fsSL https://tailscale.com/install.sh | sh
  - systemctl enable --now tailscaled
  - tailscale up --authkey=${TAILSCALE_AUTHKEY} --ssh --hostname=${HOSTNAME} || exit 1
  - /usr/local/bin/setup-k3s.sh
  - systemctl enable --now node-health.service
  - rm -f /var/lib/cloud/instance/user-data.txt

power_state:
  mode: reboot
  condition: true
  timeout: 30
